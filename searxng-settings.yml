use_default_settings: true

search:
  default_lang: ko          # 검색 언어 한국어 강제 → Google/Bing 등이 한국 사이트 우선
  ban_time_on_fail: 5
  max_ban_time_on_fail: 120
  formats:
    - html
    - json

server:
  secret_key: "rfCZz4FAjrTyAWR9Xd9f"  # SearXNG 내부 세션 키 — 외부 노출 없는 내부 전용 서비스
  bind_address: "0.0.0.0"
  port: 8080
